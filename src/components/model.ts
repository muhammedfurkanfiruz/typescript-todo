export interface Todo {
    id: number;
    todo: string;
    isDone: boolean;
}



// when we created this page we used .ts instead of .tsx because this is the just normal typescript page not react component
 